# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'WhereIsMaFood' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WhereIsMaFood
  def testing_pods
    pod 'Quick'
    pod 'Nimble'
  end
  
  target 'WhereIsMaFoodTests' do
    inherit! :search_paths
    inherited = true
    # Pods for testing
    testing_pods
  end
end
